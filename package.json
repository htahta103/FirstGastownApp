{
  "name": "todoflow",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web run test --if-present",
    "format": "npm --prefix web run format --if-present",
    "preview": "npm --prefix web run preview"
  },
  "dependencies": {},
  "devDependencies": {}
}
